import { SectionPage } from "@/components/info/InfoPages";

export const metadata = {
  title: "Conditions d'utilisation — Vidversal",
};

export default function TermsPage() {
  return <SectionPage kind="terms" />;
}